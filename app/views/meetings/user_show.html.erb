<h1><%= @meeting.title %></h1>


<p><%= @meeting.description %></p>

<%= image_tag @meeting.image_url(:large) unless @meeting.image.blank? %>

<p>Автор встречи: <b><%= @meeting.user.name %></b></p>

<p>Встреча начнется <b><%= @meeting.date_time_start %></b></p>
<p>Встреча завершится <b><%= @meeting.date_time_finish %></b></p>

<p>Встреча пройдет по адресу: <b><%= @meeting.address %></b></p>

<% if current_user == @meeting.user %>
  <%= link_to 'Редактировать', edit_user_meeting_path(@meeting) %>
<% end %>

<h2>Комментарии</h2>

<%= form_tag create_user_meeting_comment_path, method: :post do %>
  <%= text_area_tag :comment, params[:comment] %>
  <%= hidden_field_tag :comment_meeting_id, @meeting.id %>
  <%= submit_tag 'Сохранить комментарий' %>
<% end %>
<ul>
  <% @meeting.comments.each do |comment| %>
    <li><%= comment.text %> <%= "(#{comment.user.name})" if comment.user %></li>
  <% end %>
</ul>
